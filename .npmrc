# Use public npm registry for this project (override corporate proxy)
registry=https://registry.npmjs.org/

# Suppress verbose npm output
fund=false
audit=false

# Accept Playwright alpha version for Node 22 compatibility (temporary until 1.62.0 stable)
legacy-peer-deps=true
