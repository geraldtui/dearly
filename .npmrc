# Use public npm registry for this project (override corporate proxy)
registry=https://registry.npmjs.org/

# Suppress verbose npm output
fund=false
audit=false

# Skip optional dependencies that cause 403 errors
legacy-peer-deps=false
